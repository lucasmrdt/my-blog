---
title: 📖 My Notebooks
eleventyComputed:
    description: Thoughts & ramblings by {{ metadata.author.name }}
permalink: /notebooks/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber }}/{% endif %}
eleventyNavigation:
  key: Notebooks
  order: 2
pagination:
  data: collections.notebooks
  size: 4
  alias: notebooks
  reverse: true
---

{% extends 'layouts/home.njk' %}

{% block content %}
  <div class="blog-heading-tags">
    <h1>{{ title }}</h1>
    {{ component('tags-list', {
        tags: collections.tagList
    }) }}
  </div>

  {# {% imageNjk imgUrl, imgAlt, imgSize, pictureClass, imgClass, imgBorderColor %} #}

  {% set allPosts = collections.notebooks %}
  {% set postslist = notebooks %}

  {% include "layouts/partials/posts-list.njk" %}
  {% include "layouts/partials/pagination.njk" %}

{% endblock %}
